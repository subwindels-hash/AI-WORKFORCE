import Image from "next/image";
import Link from "next/link";
import { Navigation } from "../components/layout/Navigation";

const steps = [["01", "Discover", "Search live, configured business providers across the markets you serve."], ["02", "Organize", "Save source-attributed leads, build collections, and keep every action auditable."], ["03", "Prioritize", "Measure coverage, review duplicates, and move the right conversations through your pipeline."]];
const signals = [["Stable identity", "Provider + source ID prevents repeat searches from creating duplicate records."], ["Honest quality", "Coverage is calculated from stored fields. Missing information stays missing."], ["Safe export", "CSV formula-leading values are escaped before download."], ["Team ready", "Organization-scoped roles, owners, notes, and activity history are built in."]];

export default function HomePage() { return <><Navigation /><main className="mx-auto max-w-7xl px-4 py-8 sm:px-6 sm:py-12"><section className="grid overflow-hidden rounded-[2rem] border border-slate-800 bg-slate-900 shadow-2xl lg:grid-cols-[1.05fr_.95fr]"><div className="p-7 sm:p-12 lg:p-16"><div className="flex items-center gap-3"><Image src="/images/scout-logo.png" alt="Scout" width={48} height={48} className="rounded-xl" /><p className="text-sm font-semibold uppercase tracking-[.22em] text-cyan-400">Scout / Lead Intelligence</p></div><h1 className="mt-7 max-w-2xl text-4xl font-bold leading-tight text-white sm:text-6xl">Find the next business worth speaking to.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-slate-400">Scout connects real discovery results to a clean, organization-scoped sales workspace. Search, deduplicate, organize, review quality, and export without invented company data.</p><div className="mt-8 flex flex-wrap gap-3"><Link href="/app/leads" className="rounded-xl bg-cyan-400 px-5 py-3 font-semibold text-slate-950 transition hover:bg-cyan-300">Start discovery</Link><Link href="/app/lead-pipeline" className="rounded-xl border border-slate-700 px-5 py-3 font-semibold text-slate-200 transition hover:border-cyan-600">Open pipeline</Link></div><div className="mt-8 flex flex-wrap gap-2 text-xs text-slate-500"><span className="rounded-full border border-slate-700 px-3 py-1">PostgreSQL source of truth</span><span className="rounded-full border border-slate-700 px-3 py-1">Redis operational layer</span><span className="rounded-full border border-slate-700 px-3 py-1">Provider attributed</span></div></div><div className="relative min-h-[360px] lg:min-h-full"><Image src="/images/lead-discovery-hero.png" alt="Connected business locations" fill className="object-cover" priority /><div className="absolute inset-0 bg-gradient-to-r from-slate-900/40 via-transparent to-transparent" /></div></section><section className="mt-10 grid gap-4 md:grid-cols-3">{steps.map(([number, title, copy]) => <article key={number} className="rounded-2xl border border-slate-800 bg-slate-900 p-6"><p className="font-mono text-cyan-400">{number}</p><h2 className="mt-5 text-xl font-semibold text-white">{title}</h2><p className="mt-2 leading-6 text-slate-400">{copy}</p></article>)}</section><section className="mt-16 grid gap-10 lg:grid-cols-[.9fr_1.1fr] lg:items-center"><div><p className="text-xs font-semibold uppercase tracking-[.24em] text-cyan-400">Built for signal, not noise</p><h2 className="mt-3 text-3xl font-bold text-white sm:text-4xl">A sales workspace that shows its work.</h2><p className="mt-4 leading-7 text-slate-400">Every search has a ledger entry. Every status change has an activity. Every secondary duplicate is a review candidate, never an automatic merge.</p><div className="mt-6 grid gap-3 sm:grid-cols-2">{signals.map(([title, copy]) => <div key={title} className="rounded-xl border border-slate-800 bg-slate-950/70 p-4"><p className="font-semibold text-white">{title}</p><p className="mt-1 text-sm leading-5 text-slate-500">{copy}</p></div>)}</div></div><div className="relative overflow-hidden rounded-2xl border border-slate-800 bg-slate-900"><Image src="/images/customer-review.png" alt="Scout customer reviewing a lead workspace" width={1280} height={853} className="h-auto w-full object-cover" /><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-slate-950 via-slate-950/80 to-transparent p-6 pt-24"><p className="text-lg font-semibold text-white">“The team can see where every lead came from and what happens next.”</p><p className="mt-2 text-sm text-cyan-300">— Amina, growth operations</p></div></div></section><section className="mt-16 rounded-2xl border border-cyan-900/60 bg-cyan-950/20 p-7 sm:p-10"><div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.24em] text-cyan-300">Ready when your provider is</p><h2 className="mt-2 text-2xl font-bold text-white">Start with a real search.</h2><p className="mt-2 max-w-xl text-slate-400">Google Places is the first provider. It appears as disabled until its API key is configured; Scout never fills an empty provider with fake businesses.</p></div><Link href="/login" className="shrink-0 rounded-xl bg-white px-5 py-3 text-center font-semibold text-slate-950 hover:bg-cyan-100">Sign in</Link></div></section></main></>;
}
